version: '3.8'

services:
  web:
    build: .
    ports:
      - "7860:7860"
    environment:
      - GOOGLE_FACTCHECK_API_KEY=${GOOGLE_FACTCHECK_API_KEY}
    volumes:
      - ./sources.json:/app/sources.json
